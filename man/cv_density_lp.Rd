% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{cv_density_lp}
\alias{cv_density_lp}
\title{LOO cross-validation for the local polynomial density estimator}
\usage{
cv_density_lp(X, h_grid, m_grid = 0:3, domain, N_quad = 500L)
}
\arguments{
\item{X}{Numeric matrix \verb{n x d} of observations.}

\item{h_grid}{Numeric vector of bandwidth values (all > 0).}

\item{m_grid}{Integer vector of polynomial degrees (default \code{0:3}).}

\item{domain}{An \code{"domain_lp"} object.}

\item{N_quad}{Number of quadrature points per evaluation (default 500).}
}
\value{
An S3 object of class \code{"cv_density_lp"} containing:
\itemize{
\item \verb{$m_hat}, \verb{$h_hat}: selected degree and bandwidth,
\item \verb{$scores}: matrix \verb{|m_grid| x |h_grid|} of CV scores,
\item \verb{$grids}: list containing \verb{$m} and \verb{$h} grids used.
}
}
\description{
For each pair \eqn{(m, h)} in the grid, computes the leave-one-out
log-likelihood score
\deqn{\mathrm{CV}(m, h) = -\frac{1}{n}\sum_{i=1}^n
      \log \hat{f}_{\gamma,-i}(X_i)}
using the algebraic LOO correction
\deqn{\hat{f}_{\gamma,-i}(X_i) =
      \frac{n\,\hat{f}_\gamma(X_i) - h^{-d}\|H_0^{(X_i)}\|^2}{n-1},}
where \eqn{\|H_0^{(X_i)}\|^2 = (B_{X_i}^{-1})_{11}} is the self-influence
scalar returned as a byproduct of the C++ pipeline.
}
\seealso{
\code{\link[=density_lp]{density_lp()}}, \code{\link[=cv_density_lp_ppp]{cv_density_lp_ppp()}}
}
