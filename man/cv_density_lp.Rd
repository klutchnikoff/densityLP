% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R, R/methods.R
\name{cv_density_lp}
\alias{cv_density_lp}
\alias{summary.cv_density_lp}
\alias{print.cv_density_lp}
\title{LOO cross-validation for the local polynomial density estimator}
\usage{
cv_density_lp(x, h_grid, m_grid = 0:3, N_quad = 500L, ...)

\method{summary}{cv_density_lp}(object, ...)

\method{print}{cv_density_lp}(x, ...)
}
\arguments{
\item{x}{Observations. Can be a numeric \code{matrix}, a \code{spatstat.geom::ppp}
object, or a \code{pp_lp} object.}

\item{h_grid}{Numeric vector of bandwidth values (all > 0).}

\item{m_grid}{Integer vector of polynomial degrees (default \code{0:3}).}

\item{N_quad}{Number of quadrature (Monte Carlo) points used to approximate
the normalising integral (default 500).}

\item{...}{Additional arguments passed to methods.}

\item{object}{A \code{"cv_density_lp"} object (for \code{summary}).}
}
\value{
An S3 object of class \code{"cv_density_lp"} with fields:
\describe{
\item{m_hat}{Selected polynomial degree.}
\item{h_hat}{Selected bandwidth.}
\item{scores}{Matrix of CV scores (rows = degrees, columns = bandwidths).}
\item{grids}{List with \code{$m} and \code{$h} grids used.}
}
}
\description{
For each pair \eqn{(m, h)} in the grid, computes the leave-one-out
log-likelihood score
\deqn{\mathrm{CV}(m, h) = -\frac{1}{n}\sum_{i=1}^n
      \log \hat{f}_{\gamma,-i}(X_i)}
using the algebraic LOO correction
\deqn{\hat{f}_{\gamma,-i}(X_i) =
      \frac{n\,\hat{f}_\gamma(X_i) - h^{-d}\|H_0^{(X_i)}\|^2}{n-1},}
where \eqn{\|H_0^{(X_i)}\|^2 = (B_{X_i}^{-1})_{11}} is the self-influence
scalar returned as a byproduct of the C++ pipeline.
}
\examples{
set.seed(1)
X <- matrix(runif(30 * 2), ncol = 2)
dom <- domain_Rd(2L)
cv <- cv_density_lp(X, h_grid = c(0.2, 0.4), m_grid = 0:1, domain = dom, N_quad = 100L)
print(cv)
summary(cv)
}
\seealso{
\code{\link[=density_lp]{density_lp()}}
}
