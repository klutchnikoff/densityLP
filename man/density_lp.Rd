% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_lp.R, R/methods.R
\name{density_lp}
\alias{density_lp}
\alias{print.density_lp}
\alias{summary.density_lp}
\alias{plot.density_lp}
\title{Local polynomial density estimation}
\usage{
density_lp(x, h, m = 0L, t_grid = NULL, N_quad = 500L, ...)

\method{print}{density_lp}(x, ...)

\method{summary}{density_lp}(object, ...)

\method{plot}{density_lp}(x, ...)
}
\arguments{
\item{x}{Observations. Can be a numeric \code{matrix}, a \code{spatstat.geom::ppp}
object, or a \code{pp_lp} object.}

\item{h}{Bandwidth (scalar > 0).}

\item{m}{Polynomial degree (integer >= 0, default 0).}

\item{t_grid}{Numeric matrix \code{p x d} of target points. If \code{NULL},
it is automatically generated from the bounding box of \code{x}.}

\item{N_quad}{Number of quadrature (Monte Carlo) points used to approximate
the normalising integral (default 500).}

\item{...}{Additional arguments passed to methods or for grid generation
(\code{nx}, \code{ny} for 2D).}

\item{object}{A \code{"density_lp"} object (for \code{summary}).}
}
\value{
An S3 object of class \code{"density_lp"}. In 2D, it also inherits
from \code{spatstat.geom::im}.
}
\description{
Local polynomial density estimation
}
\examples{
# 2D estimation on the unit square
set.seed(1)
X <- matrix(runif(50 * 2), ncol = 2)
dom <- domain_Rd(2L)
fit <- density_lp(X, h = 0.3, m = 1L, domain = dom, nx = 10L, ny = 10L, N_quad = 100L)
print(fit)
summary(fit)

# 1D estimation with plot
set.seed(2)
X1 <- matrix(runif(50), ncol = 1)
t1 <- matrix(seq(0.1, 0.9, length.out = 20), ncol = 1)
fit1 <- density_lp(X1, h = 0.3, domain = domain_Rd(1L), t_grid = t1, N_quad = 100L)
plot(fit1)

}
