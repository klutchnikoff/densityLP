% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density_lp.R
\name{density_lp}
\alias{density_lp}
\title{Local polynomial density estimation on a grid of target points}
\usage{
density_lp(X, t_grid, h, m = 0L, domain, N_quad = 500L)
}
\arguments{
\item{X}{Numeric matrix \verb{n x d} of observations. Must be a matrix; no
implicit conversion from data frames or tibbles is performed.}

\item{t_grid}{Numeric matrix \verb{p x d} of target points.}

\item{h}{Bandwidth (scalar > 0).}

\item{m}{Polynomial degree (integer >= 0).}

\item{domain}{An \code{"domain_lp"} object describing the support of the density
(see \code{\link[=domain_Rd]{domain_Rd()}}, \code{\link[=domain_from_indicator]{domain_from_indicator()}}, \code{\link[=domain_sector]{domain_sector()}}).}

\item{N_quad}{Number of Monte Carlo quadrature points per target point
(default: 500).}
}
\value{
An S3 object of class \code{"density_lp"}.
}
\description{
Local polynomial density estimation on a grid of target points
}
\examples{
set.seed(1)
X <- matrix(runif(50 * 2), ncol = 2)
t_grid <- as.matrix(expand.grid(
  x = seq(0.1, 0.9, length.out = 5),
  y = seq(0.1, 0.9, length.out = 5)
))
dom <- domain_Rd(2)
fit <- density_lp(X, t_grid = t_grid, h = 0.3, m = 1L, domain = dom, N_quad = 100L)
print(fit)

}
